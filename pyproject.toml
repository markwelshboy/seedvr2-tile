[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "seedvr2-tile"
version = "0.5.0"
description = "Standalone spatially tiled batch upscaling frontend for SeedVR2"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Mark Richards"}]
dependencies = [
  "numpy>=1.24",
  "Pillow>=10.0",
  "opencv-python>=4.8",
]

[project.scripts]
seedvr2-tile = "seedvr2_tile.cli_noise:main"
seedvr2-sweep = "seedvr2_tile.interactive_probe_report:probe_main"
seedvr2-sweep-report = "seedvr2_tile.interactive_probe_report:report_main"
seedvr2-sweep-full = "seedvr2_tile.tile_cap_sweep:main"
seedvr2-sweep-full-report = "seedvr2_tile.tile_cap_sweep:report_main"
seedvr2-sweep-serve = "seedvr2_tile.report_server:main"

[project.urls]
Repository = "https://github.com/markwelshboy/seedvr2-tile"
SeedVR2 = "https://github.com/numz/ComfyUI-SeedVR2_VideoUpscaler"

[tool.setuptools.packages.find]
include = ["seedvr2_tile*"]
